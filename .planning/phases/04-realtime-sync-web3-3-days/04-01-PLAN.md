---
phase: 04-realtime-sync-web3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/components/admin/AllocationPreview.tsx
  - src/app/components/admin/GasFeeSummary.tsx
  - src/app/lib/gasUtils.ts
  - src/app/hooks/useGasPrice.ts
autonomous: true
requirements:
  - REQ-302
user_setup: []

must_haves:
  truths:
    - "User sees estimated gas fee before submission"
    - "Gas fee updates when form values change"
    - "Fee display shows ETH amount in readable format"
  artifacts:
    - path: "src/app/components/admin/GasFeeSummary.tsx"
      provides: "Gas fee display component"
    - path: "src/app/lib/gasUtils.ts"
      provides: "estimateGas() function"
    - path: "src/app/hooks/useGasPrice.ts"
      provides: "useGasPrice hook with polling"
  key_links:
    - from: "DistributionForm.tsx"
      to: "GasFeeSummary.tsx"
      via: "props: estimatedFee, isEstimating"
    - from: "AllocationPreview.tsx"
      to: "GasFeeSummary.tsx"
      via: "rendered below allocation breakdown"
---

<objective>
Implement gas estimation and display in the distribution form. Users should see the estimated transaction fee (in ETH) based on current gas prices before submitting a distribution.

Purpose: Transparency in transaction costs; prevents user surprise at signing.
Output: GasFeeSummary component, gas estimation utility functions
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-realtime-sync-web3-3-days/04-RESEARCH.md
</execution_context>

<context>
Key interfaces from Phase 3:
```typescript
interface DistributionPayload {
  project_id: string;
  recipients: { user_id: string; percentage: number }[];
  total_amount: number;
  initiated_by?: string;
}
```

Current AllocationPreview renders breakdown with calculated amounts.
Phase 04 Plan 01 adds gas fee row below the breakdown.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GasFeeSummary component</name>
  <files>src/app/components/admin/GasFeeSummary.tsx</files>
  <action>
Create a new component that displays estimated gas fee. Props:
  - estimatedFeeEth: string (e.g., "0.0045")
  - isEstimating: boolean (show spinner while loading)
  - gasPrice: string (e.g., "45 gwei")

Component should:
  - Display "Estimated Gas Fee: 0.0045 ETH (~$X)"
  - Show gas price in parentheses for info
  - Use dark theme styling (slate-700 background, white text, yellow accent for warning)
  - Show "Calculating..." spinner if isEstimating=true
  - Include warning: "Gas price estimates from blockchain; actual may vary"

Use tailwind classes: bg-slate-700/40, border border-yellow-500/20, rounded-lg, p-4
Include icons: ⚡ for gas, ⏱️ for time estimate
  </action>
  <verify>
    <automated>grep -r "GasFeeSummary" src/app/components/admin/ && test -f src/app/components/admin/GasFeeSummary.tsx</automated>
  </verify>
  <done>Component exports GasFeeSummary as default; accepts all 3 props; renders fee with warning text</done>
</task>

<task type="auto">
  <name>Task 2: Create gasUtils.ts with estimateGas function</name>
  <files>src/app/lib/gasUtils.ts</files>
  <action>
Create utility functions for gas estimation:

1. estimateGas(recipients: {user_id, percentage}[], totalAmount: number): Promise<{
     gasLimit: bigint;
     gasPrice: bigint;
     totalFeeWei: bigint;
     totalFeeEth: string;
   }>
   - Call ethers getGasPrice() to get current gas price
   - Import RevenueRights contract ABI
   - Call contract.estimateGas.distributeRevenue([addresses], [amounts])
   - Convert to ETH using ethers.formatEther()
   - Return all values for display

2. formatGasPrice(gasPriceWei: bigint): string
   - Convert wei to gwei for display (divide by 1e9)
   - Return e.g., "45 gwei"

3. getEthUsdPrice(): Promise<number>
   - For display purposes, fetch current ETH price (mock: return 2500)
   - Called once on component mount, cached for 5 minutes

Error handling: If contract.estimateGas fails (insufficient funds, etc), catch and return null
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep "GasFeeSummary\|gasUtils" | head -5</automated>
  </verify>
  <done>estimateGas exported and callable; returns correct structure with gaslimit, gasprice, fee; handles errors gracefully</done>
</task>

<task type="auto">
  <name>Task 3: Create useGasPrice hook</name>
  <files>src/app/hooks/useGasPrice.ts</files>
  <action>
Create React hook that polls gas prices and triggers re-estimation:

useGasPrice(shouldPoll: boolean = true): {
  gasPrice: string;
  estimatedFee: string;
  isEstimating: boolean;
  error: string | null;
}

Implementation:
  - useEffect: On mount, call getGasPrice() immediately
  - setInterval: Poll every 10 seconds while shouldPoll=true
  - Return current gasPrice + estimatedFee (computed from latest gas price)
  - Clean up interval on unmount
  - If error occurs, set error state but keep showing last valid estimate
  - Show isEstimating spinner on first load and during refetch

Usage in AllocationPreview:
  const { gasPrice, estimatedFee, isEstimating } = useGasPrice();
  <GasFeeSummary estimatedFeeEth={estimatedFee} gasPrice={gasPrice} isEstimating={isEstimating} />
  </action>
  <verify>
    <automated>grep -n "export.*useGasPrice" src/app/hooks/useGasPrice.ts</automated>
  </verify>
  <done>Hook exported and properly typed; fetches gas price on mount; polls every 10s; cleans up interval</done>
</task>

<task type="auto">
  <name>Task 4: Integrate GasFeeSummary into AllocationPreview</name>
  <files>src/app/components/admin/AllocationPreview.tsx</files>
  <action>
Modify AllocationPreview component:

1. Import GasFeeSummary and useGasPrice hook
2. Add call hook in component: const { gasPrice, estimatedFee, isEstimating } = useGasPrice()
3. Pass recipients and totalAmount to hook for estimation context
4. Render GasFeeSummary below the allocation breakdown table:
   ```
   <div className="mt-4">
     <GasFeeSummary 
       estimatedFeeEth={estimatedFee} 
       gasPrice={gasPrice} 
       isEstimating={isEstimating} 
     />
   </div>
   ```
5. Maintain existing warning footer ("Review carefully..." text)
6. Ensure styling consistency with dark theme

DO NOT change the allocation calculation logic; only add the gas summary component below it.
  </action>
  <verify>
    <automated>grep -c "GasFeeSummary\|useGasPrice" src/app/components/admin/AllocationPreview.tsx</automated>
  </verify>
  <done>GasFeeSummary integrated below allocation table; hook called and connected; component still exports default React.memo</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ Blockchain | Gas price from public RPC; could be outdated but not malicious |
| User ↔ MetaMask | User verifies actual fee in MetaMask signing dialog |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | gasUtils.ts | mitigate | Display estimated fee prominently; crypto conversions transparent (formatEther built-in) |
| T-04-02 | Denial of Service | useGasPrice hook | mitigate | Polling interval 10s minimum; fail gracefully if RPC unavailable (show last valid price) |
| T-04-03 | Tampering | Gas price display | accept | User can verify in MetaMask dialog; purely informational (not enforced) |

</threat_model>

<verification>
After all tasks complete:
1. DistributionForm displays gas fee below allocation preview
2. Gas fee updates every 10 seconds
3. Fee displays in ETH with readable format
4. No "undefined" or NaN display for edge cases
5. Error messages show if gas estimation fails
</verification>

<success_criteria>
- GasFeeSummary component renders without errors
- estimateGas() returns correct fee structure
- useGasPrice hook polls and updates every 10 seconds
- AllocationPreview shows fee below allocation table
- No TypeScript errors in build
</success_criteria>

<output>
After completion, create `.planning/phases/04-realtime-sync-web3-3-days/04-01-SUMMARY.md`
</output>
