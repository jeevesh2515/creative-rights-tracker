---
phase: 04-realtime-sync-web3
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/app/api/admin/distribute/route.ts
  - src/app/components/admin/DistributionForm.tsx
  - src/app/components/admin/TransactionStatusDialog.tsx
  - src/app/lib/transactionUtils.ts
autonomous: true
requirements:
  - REQ-301
user_setup: []

must_haves:
  truths:
    - "Distribution form submission calls contract.distributeRevenue()"
    - "Transaction hash returned and stored in database"
    - "User sees 'pending' status while transaction awaits confirmation"
    - "UI updates to 'confirmed' when transaction confirmed on-chain"
  artifacts:
    - path: "src/app/api/admin/distribute/route.ts"
      provides: "Enhanced POST endpoint that calls contract"
    - path: "src/app/components/admin/TransactionStatusDialog.tsx"
      provides: "Modal showing transaction status (pending/confirming/confirmed)"
    - path: "src/app/lib/transactionUtils.ts"
      provides: "parseTransactionError(), formatTxHash() utilities"
  key_links:
    - from: "DistributionForm.tsx"
      to: "POST /api/admin/distribute"
      via: "form submit calls API with full payload"
    - from: "POST /api/admin/distribute"
      to: "contract.distributeRevenue()"
      via: "server calls contract with recipients[] and amounts[]"
    - from: "POST response"
      to: "TransactionStatusDialog.tsx"
      via: "returns txHash; dialog polls for confirmation"
---

<objective>
Implement blockchain transaction submission. When admin submits a distribution, call the smart contract, get a transaction hash, and track confirmation status.

Purpose: Move from database-only approach to blockchain-finalized distributions
Output: Enhanced POST endpoint, status tracking UI, transaction utilities
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-realtime-sync-web3-3-days/04-RESEARCH.md
@.planning/phases/04-realtime-sync-web3-3-days/04-01-PLAN.md
</execution_context>

<context>
From Phase 3 POST /api/admin/distribute:
- Receives DistributionPayload with recipients[] and amounts[]
- Currently: Creates distributions + distribution_recipients + activity log entries
- Phase 04 addition: Call contract.distributeRevenue() first, get tx hash

Transaction status states: pending (0 conf) → confirming (1-4 conf) → confirmed (5+ conf)

Contract interface (from Phase 1 types.ts):
```typescript
export interface IRevenueRights {
  distributeRevenue(recipients: string[], amounts: bigint[]): Promise<ContractTransactionResponse>;
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TransactionStatusDialog component</name>
  <files>src/app/components/admin/TransactionStatusDialog.tsx</files>
  <action>
Create modal dialog that shows transaction status. Props:
  - isOpen: boolean
  - txHash: string (e.g., "0x1234...")
  - status: 'pending' | 'confirming' | 'confirmed' | 'failed'
  - errorMessage?: string
  - onClose: () => void

Component renders:
  - Header: "Transaction Status"
  - Hash display: "0x1234...5678" (truncated with copy button)
  - Status badge with icon:
    * ⏳ pending/"Awaiting confirmation"
    * 🔗 confirming/"Confirming (X/5 blocks)"
    * ✅ confirmed/"Confirmed!"
    * ❌ failed/"Failed: {errorMessage}"
  - If pending/confirming: Show spinner + "Checking blockchain..."
  - If confirmed: Show "Distribution completed" + close button
  - If failed: Show red error box + "Retry" button (optional)
  - All styling: dark theme (border-yellow-500 for pending, border-green-500 for confirmed, border-red-500 for failed)

Copy tx hash to clipboard on click.
  </action>
  <verify>
    <automated>test -f src/app/components/admin/TransactionStatusDialog.tsx && grep -q "export.*TransactionStatusDialog" src/app/components/admin/TransactionStatusDialog.tsx</automated>
  </verify>
  <done>Component exported; accepts all props; renders status badge and hash correctly</done>
</task>

<task type="auto">
  <name>Task 2: Create transactionUtils.ts for error parsing</name>
  <files>src/app/lib/transactionUtils.ts</files>
  <action>
Create utility functions for transaction handling:

1. parseTransactionError(error: any): {
     code: string;
     message: string;
     recoverable: boolean;
   }
   - Extract error.code: 'ACTION_REJECTED' (user declined), 'CALL_EXCEPTION' (revert), 'NETWORK_ERROR', etc.
   - Extract error.reason from revert reason if available
   - Map to human-readable message
   - Return recoverable=true for network errors, false for user rejection

2. formatTxHash(hash: string, length: number = 8): string
   - Truncate to "0x1234...5678" style
   - Used in UI display

3. calculateConfirmations(currentBlockNumber: number, txBlockNumber: number): number
   - Simple subtraction for display

Error mappings:
  - ACTION_REJECTED → "You rejected the transaction in MetaMask"
  - CALL_EXCEPTION → "Contract call failed: {reason}"
  - INSUFFICIENT_DATA → "Network error; please retry"
  - TIMEOUT, NETWORK → "Network delay; transaction may still process"
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -q "transactionUtils"</automated>
  </verify>
  <done>All 3 functions exported; error code mappings comprehensive; formatTxHash truncates correctly</done>
</task>

<task type="auto">
  <name>Task 3: Enhance POST /api/admin/distribute to call contract</name>
  <files>src/app/api/admin/distribute/route.ts</files>
  <action>
Modify existing POST endpoint to:

1. Keep all existing validation (percentages, amounts, auth checks)

2. NEW: Before creating DB records, call contract:
   ```
   const contract = getRevenueRightsContract(signer); // signer from session/wallet
   const recipientAddresses = recipients.map(r => r.user_id); // Assuming user_id is address
   const amounts = recipients.map(r => ethers.parseEther(r.calculated_amount.toString()));
   
   const tx = await contract.distributeRevenue(recipientAddresses, amounts);
   const txHash = tx.hash;
   ```

3. If contract call fails, catch error and return 400 with error message (don't create DB records)

4. If contract call succeeds:
   - Create distribution with status='pending', txHash, blockNumber=null
   - Create distribution_recipients as before
   - Log to activity: action='distribute_revenue', txHash=txHash, status='pending'

5. Return 201 with:
   ```
   {
     distributionId: string;
     txHash: string;
     status: 'pending';
     recipient_count: number;
   }
   ```

Error responses:
  - 400 if contract call fails (include error reason)
  - 403 if not admin
  - 401 if unauthenticated
  - 500 on unexpected errors

Add try/catch around contract call specifically:
  try {
    const tx = await contract.distributeRevenue(...);
  } catch (error) {
    // Parse with parseTransactionError utility
    return NextResponse.json({ error: parsedError.message }, { status: 400 });
  }
  </action>
  <verify>
    <automated>grep -n "contract.distributeRevenue\|txHash\|status.*pending" src/app/api/admin/distribute/route.ts | wc -l</automated>
  </verify>
  <done>Contract call implemented; txHash captured; DB stores pending status; error handling in place; returns 201 with txHash</done>
</task>

<task type="auto">
  <name>Task 4: Integrate TransactionStatusDialog into DistributionForm</name>
  <files>src/app/components/admin/DistributionForm.tsx</files>
  <action>
Modify DistributionForm to show status dialog after submission:

1. Add state: const [transactionState, setTransactionState] = useState<{
     isOpen: boolean;
     txHash?: string;
     status?: 'pending' | 'confirming' | 'confirmed' | 'failed';
     error?: string;
   }>

2. In handleSubmit(), after successful POST response:
   - Extract txHash from response
   - setTransactionState({ isOpen: true, txHash, status: 'pending' })
   - Start polling for confirmation: fetch(`/api/admin/distributions/{id}`) every 10 seconds
   - On status change (pending → confirmed), update to 'confirmed'
   - Stop polling when confirmed or after 5 minute timeout

3. Show dialog: 
   <TransactionStatusDialog 
     isOpen={transactionState.isOpen} 
     txHash={transactionState.txHash} 
     status={transactionState.status}
     onClose={() => setTransactionState({ isOpen: false })}
   />

4. When form submits:
   - Show "Waiting for blockchain confirmation..."
   - Disable retry button until (confirmed | failed | timeout)
   - Keep form values but prevent edit during submission

DO NOT change form validation or preview logic; only add dialog integration.
  </action>
  <verify>
    <automated>grep -c "TransactionStatusDialog\|transactionState" src/app/components/admin/DistributionForm.tsx</automated>
  </verify>
  <done>Dialog integrated; state management for txHash/status; polling implemented; user sees status updates</done>
</task>

<task type="auto">
  <name>Task 5: Create API endpoint to poll distribution status</name>
  <files>src/app/api/admin/distributions/[id]/route.ts</files>
  <action>
Create new GET endpoint for fetching distribution status:

GET /api/admin/distributions/{id}
  - Returns: { id, status, txHash, blockNumber, confirmations, created_at }
  - Query DB distributions table for the record
  - If txHash exists:
    * Call ethers provider.getTransactionReceipt(txHash)
    * If receipt exists: set status = 'confirmed', blockNumber from receipt
    * Calculate confirmations = currentBlockNumber - txBlockNumber
  - If no receipt yet: status = 'pending'
  - Response: 200 with distribution object

Error handling:
  - 404 if distribution not found
  - 403 if user not admin and doesn't own distribution

This endpoint is called by DistributionForm polling to update status.
  </action>
  <verify>
    <automated>test -f src/app/api/admin/distributions/[id]/route.ts && grep -q "GET\|provider.getTransactionReceipt" src/app/api/admin/distributions/[id]/route.ts</automated>
  </verify>
  <done>GET endpoint created; checks receipt from blockchain; calculates confirmations; returns status correctly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ API | Tx hash returned by API; user can verify in Etherscan |
| API ↔ Contract | Contract call is sovereign; API just triggers it |
| API ↔ Database | All tx hashes stored immutably for audit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | txHash storage | mitigate | Store in immutable activity table; log original hash |
| T-04-05 | Repudiation | Transaction lifecycle | mitigate | Audit table tracks status changes; block numbers immutable |
| T-04-06 | Denial of Service | Transaction polling | mitigate | Polling interval 10s; timeout after 5 min of pending |

</threat_model>

<verification>
After all tasks complete:
1. Submit distribution form → see "pending" status dialog
2. Check (via Etherscan or test tool) transaction is on-chain
3. Dialog updates to "confirmed" within 1 minute
4. Distribution record has txHash and block number
5. Activity log shows status='pending' initially, then 'confirmed'
</verification>

<success_criteria>
- POST /api/admin/distribute calls contract.distributeRevenue()
- TransactionStatusDialog shows pending/confirming/confirmed states
- API returns txHash and status='pending' on success
- Client polls for confirmation and updates dialog
- Error messages are human-readable
- No TypeScript errors in build
</success_criteria>

<output>
After completion, create `.planning/phases/04-realtime-sync-web3-3-days/04-02-SUMMARY.md`
</output>
